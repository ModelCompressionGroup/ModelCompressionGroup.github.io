/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "ampleai.io", "https://ampleai.io"],
    remotePatterns: [],
  },
};

module.exports = nextConfig;
